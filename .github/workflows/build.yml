name: build-amr

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.12.4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Ensure qjsc binaries are executable
        run: chmod +x aiot-vue-cli/cli-libs/aiot-qjsc-tool/assets/bin/linux/*

      - name: Run unit tests
        run: pnpm test

      - name: Compile native module (armv7-glibc) and install into ui/libs
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq g++-arm-linux-gnueabihf
          chmod +x tools/build-native.sh
          tools/build-native.sh --prefix arm-linux-gnueabihf- --abi armv7-glibc --install

      - name: Build AMR
        run: pnpm -C ui package

      - name: Upload AMR artifact
        uses: actions/upload-artifact@v4
        with:
          name: better-video-player-amr
          path: ui/*.amr
          if-no-files-found: error
